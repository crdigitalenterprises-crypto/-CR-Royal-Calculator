import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cr.royalcalculator',
  appName: 'CR Royal Calculator',
  webDir: 'dist'
};

export default config;
